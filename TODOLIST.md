1、页面布局：侧边栏 + 内容区域。
2、侧边栏菜单有：数据面板bashboard（展示应用的主要数据）、书籍目录books（展示书籍列表）、AI角色（展示AI发音人列表）、系统设置。
3、每个侧边栏菜单都有自己的路由。下面的数据详情页、章节详情页页都有自己的路由和id
4、进入书籍列表，展示的是书籍列表，不用card组件展示，用table列表展示，可以搜索、筛选，每一条是一本数据，每一条的列有：序号、书籍名称、备注、状态、创建时间、操作，操作列目前设置为“进入”、“角色”、“设置”；
   【进入】 是进入书籍详情，详情是章节列表；
   【角色】 是进入此书籍的角色配置页面；
   【设置】 是一个弹框，弹框里是书籍的基本设置，包括书籍名称、备注等等；
   操作列的 进入、角色 都是访问书籍详情页。
5、书籍详情页里，主页面的侧边栏还是显示的，并且定位(active)到书籍目录菜单，右侧内容区上面有两个tab：章节、角色，分别对应 从书籍列表页的操作列的进入和角色；两个tab同一行的页面的最右边是批量操作下拉框（批量导出画本、批量导出成品）、画本导入、新建章节 按钮；详情页的章节tab是此书籍的章节列表，角色tab是此书籍的角色列表；章节列表的列有：拖动排序的图标、批量勾选的图标、序号、章节名称、章节字数、状态（初始化、编辑中、已合成）、编辑时间、操作，操作列有：进入、导出画本、导出成品；
6、章节详情页，是从书籍详情页的章节列表的每一行的操作列的进入打开的页面，
章节详情里，最上面一行的区域里：左边是一个返回按钮，是返回到书籍详情页（也就是书籍章节列表页面，返回按钮旁边显示当前书籍名称），中间是章节&章节名称，最右边是三个按钮：保存、上一章节、下一章节；
然后紧接着是 导入文本、新增段落、加载发音人、一键合成、导出成品 等按钮；
然后下面就是段落列表，每一个段落的显示左右布局：最左边是角色选择的下拉框，用来选择是旁白还是哪一个角色，然后是为选择的角色配置Ai发音人的下拉框；然后右边的布局里上下布局，最上面是段落显示是输入框和内容，下一行是目前布局的那些，语速、音调、情感、自然语言指令，下一行是 试听、合成，注意这里的保存没有了，不再保存段落了，改成整章节保存，就是最上面右上角的保存。
